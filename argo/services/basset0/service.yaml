apiVersion: v1
kind: Service
metadata:
  name: basset0
  labels:
    app.kubernetes.io/name: basset0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: basset0
  ports:
    - name: http
      port: 80
      targetPort: http
